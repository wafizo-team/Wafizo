# ── Base de données (PostgreSQL via docker-compose.dev.yml) ──
DATABASE_URL="postgresql://wafizo:wafizo@localhost:5432/wafizo_dev?schema=public"

# ── Redis (BullMQ via docker-compose.dev.yml) ──
REDIS_URL="redis://localhost:6379"
